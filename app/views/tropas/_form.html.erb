<%= form_with(model: tropa, local: true) do |form| %>
  <% if tropa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tropa.errors.count, "error") %> prohibited this tropa from being saved:</h2>

      <ul>
      <% tropa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :dob %>
    <%= form.date_select :dob %>
  </div>

  <div class="field">
    <%= form.label :contactNo %>
    <%= form.text_area :contactNo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
